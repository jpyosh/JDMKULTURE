# Brand assets

Drop the exact shop logo here as:

`jdm-kulture-logo.png`

Recommended format: PNG with a transparent background, ideally at least 512x512 pixels.

After adding the image, update the logo source in `public/index.html` from `jdm-mark.svg` to `assets/jdm-kulture-logo.png` if needed.
